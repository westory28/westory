# Staging 업로드 및 사전 안정화 패치

2026-09-10. 작업 위치는 `C:\westory-w10p`, 브랜치는 `codex/phase6-w10p-presentation-freeze`입니다. 운영 프로젝트와 main에는 접근·배포하지 않습니다. 테스트 환경은 `westory-staging-177587430482`이며, 테스트 관리자 계정은 사용자가 확인한 `westoria28@gmail.com`입니다.

## 변경 내용

- 수업자료 판서 도구·저장·복원 제거, 빈칸 답안 저장·정오답 표시, 핵심포인트 찾기·보상, 이미지·글상자·링크 각주를 유지한 앞선 패치를 이어갑니다. 기존 판서 데이터는 삭제하지 않습니다.
- 실제 Staging에서 직접 Storage 업로드가 권한 오류로 실패했습니다. 로컬 에뮬레이터 통과를 실제 서버 성공으로 취급하지 않습니다. Storage Rules의 Firestore 조회 제한 때문에 파일 전송을 `uploadLessonAssetContent`로 옮겼습니다.
- 서버가 App Check, application session, 최근 인증, 교사 권한, ticket 소유권·만료·학기·크기·SHA256을 검증합니다. Storage는 `ifGenerationMatch: 0`으로 최초 생성만 허용하며 중복 전송으로 기존 파일을 덮어쓰지 않습니다. 수업에 연결하는 Gateway 명령과 finalize 검증은 유지합니다. 클라이언트의 해당 경로 직접 쓰기는 모두 차단합니다.
- 다른 교사가 올린 수업 PDF의 재추출은 기존 수업에서 사용하는 저장된 다운로드 URL을 재사용합니다. 현재 bucket 및 경로를 정확히 대조하고, HTTPS·token·20MB 스트리밍 제한을 적용하며 리디렉션은 거부합니다. 새 파일 저장과 수업 연결은 서버 권한·revision 검증을 다시 거칩니다.
- 앞선 `7746bd3`에서는 역사 사전 일괄 등록의 기존 단어 덮어쓰기를 차단하고, 요청 처리 쿼리에 학기·상태 조건 및 초과 감지를 추가했습니다. DIC 전체 Gateway 이전 완료를 뜻하지 않습니다.

## 검증 근거

- 업로드 전송 단위 검사 22건, PDF 다운로드 검사 8건, 기존 finalize 검사 16건 통과.
- Firestore/Storage Rules 검사 74건 통과. 에뮬레이터 종료 시 Java NPE 로그가 발생했지만 테스트와 명령은 exit 0이었으며, 남은 해당 프로젝트 에뮬레이터 프로세스를 확인해 종료했습니다.
- Functions 전체 check 및 직접 쓰기 경계 검사 통과. 승인 경계 136개, UNKNOWN 0. 타입 기준선은 기존 44오류/12파일이며 신규 오류 0입니다.
- 독립 읽기 전용 검토가 발견한 다른 교사 PDF 재추출 및 점검 중 재추출 문제를 수정했고, 재검토에서 추가 P0/P1은 확인되지 않았습니다.
- 실제 Staging 결과와 Vercel 배포 소스 해시는 별도 배포 보고서에서 최종 확인합니다. 이 문서만으로 실제 업로드·로그인·보상·전체 화면 검증 완료를 선언하지 않습니다.

## 본 사이트 반영 전 남은 조건

### 실제 관리자 로그인에서 추가로 발견한 오류

`44ee024`의 Staging 배포·파일 대조는 통과했지만, 실제 Chrome 관리자 로그인은 Google에서 돌아온 뒤 인증 결과를 잃어 ‘로그인이 취소되었거나 중간에 돌아왔습니다’로 종료됐습니다. HTTPS 전체에 redirect를 강제하던 코드와 서로 다른 Vercel/Firebase 인증 도메인이 영향을 주는 경로입니다. [Firebase의 redirect 권고](https://firebase.google.com/docs/auth/web/redirect-best-practices)의 팝업 방식을 적용하여 **Staging의 교차 인증 도메인인 경우에만** 팝업을 사용하고, 팝업 실패를 다시 교차 도메인 redirect로 보내지 않도록 수정했습니다. 실제 사용자 로그인 재검증 전까지 관리자 접속 완료를 선언하지 않습니다. Production 및 localhost의 기존 선택 경로는 유지합니다.

실제 브라우저의 재인증·여러 화면 복구, 전체 화면 계약 검증, DIC/PATCH 명령 이전과 지도·사료·XLSX 운영 정책, 실제 명단·학기 일정, 백업·복원 실증 및 운영 전용 전환·롤백 검토가 남습니다. 테스트 사이트를 열어 확인할 수 있는 상태와 본 사이트 배포 준비 완료는 구분합니다. 목표일은 2026-09-16(수)이며 확정 배포일은 아닙니다.
