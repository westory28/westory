# 교사 패치 메모 입력 보호 — 2026-09-11

기준 소스는 `1b4f5b133ba3fd6aadbaa7912e4d12185475f8b2`입니다. 이번 변경은 PATCH01 서버 저장 전환에 앞서 확인된 클라이언트 입력 손실과 목록 복구 문제를 좁혀 수정합니다.

## 변경

- 메모 패널 닫기·재열기·Esc는 작성 중인 내용을 유지합니다.
- 저장·상태 변경·삭제가 진행되는 동안 폼과 다른 메모 조작을 잠급니다. 동기 ref로 연속 클릭도 차단합니다.
- 다른 메모나 새 메모로 전환할 때 변경한 입력이 있으면 폐기 여부를 확인합니다.
- 계정 또는 교사 권한이 바뀌면 개인 초안을 분리하고, 이전 계정의 늦은 저장 응답은 새 초안과 알림에 영향을 주지 않습니다.
- 목록 조회 오류를 빈 목록과 구분하며 다시 불러올 수 있습니다. 재시도는 입력을 지우지 않습니다.
- 본문 수정 요청은 처리 상태와 완료 시각을 보내지 않습니다. 다른 창의 완료 처리와 오래된 상태가 충돌해 Rules에서 저장을 거절하는 경로를 제거합니다.

## 검증

`node scripts/verify-teacher-patch-memo-recovery.mjs`는 실제 컴포넌트와 실제 데이터 helper를 격리 실행합니다. 지연·실패·재시도, 닫기·Esc, 초안 폐기 취소/확인, 계정·권한 전환, 늦은 응답, 목록 재구독, 상태 필드 분리를 포함한 **75개 검사 통과**입니다. 화면 폭은 **390·768·1280·1440px**, 런타임 오류와 가로 넘침은 0입니다. 외부 네트워크와 Firebase는 차단한 합성 테스트이며, 실제 서버 저장·재인증 증거로 대신하지 않습니다.

출력: `C:\Users\방재석\AppData\Local\Temp\westory-memo-recovery-KqDUWY`.

앱 빌드 통과, TypeScript 기존 오류 44개/12개 파일 대비 신규 0개입니다. 읽기 전용 독립 검토에서 이번 diff의 추가 P0/P1/P2는 확인되지 않았습니다. 배포와 후속 검증 결과는 사용자 인수인계에 기록합니다.

직접 쓰기 검사에서는 UI 콜백 위치 변경으로 DW-053·DW-055 호출 그래프 해시만 갱신했습니다. 각각 기존 본인 메모 삭제·상태 변경 사용자 이벤트 1개이며, API·경로·허용 범위·W12 이전 기한은 그대로입니다. 신규 쓰기 경로를 허용하지 않았습니다.

## 남은 출시 조건

- PATCH01 Gateway·receipt·서버 CAS 전환: 여러 창의 본문 저장 충돌, 응답 유실 후 정확히 한 번 저장.
- 최신 100건 밖의 미처리 메모 조회와 페이지 처리.
- 실제 재인증·세션 복구 및 구버전 번들의 직접 쓰기 차단 검증.
- 학생 빈칸 재진입과 실제 보상 지급의 중복 방지 검증, DIC01/DIC02 및 기존 출시 조건.

초안은 현재 페이지의 메모리에만 보관합니다. 새로고침·탭 종료까지 보존하는 기능은 이번 변경에 포함하지 않습니다. Production 접근·변경·배포는 0이며, 전용 Staging과 합의된 기능 브랜치만 사용합니다.
