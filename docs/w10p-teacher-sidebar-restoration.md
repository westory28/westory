# 교사 사이드 메뉴 복구와 수업 제시 화면 제거

2026-09-10 사용자 후속 요청입니다. 실제 작업은 `C:\westory-w10p`의 기능 브랜치에서 진행하며, 운영 프로젝트와 main은 이번 배포 대상이 아닙니다.

- 별도의 교사용 수업 화면, 진입 버튼, 지연 로딩 컴포넌트를 제거합니다. PDF 편집 및 학생 미리보기, 빈칸 답안과 정오답 효과, 핵심포인트 찾기·보상, 이미지·글상자·링크 각주는 유지합니다.
- 기존 필기 데이터는 더 이상 보존 필수 대상이 아닙니다. 테스트 환경의 `lesson_presentations` 아래 전용 문서만 정리 대상으로 삼고, 운영 데이터 삭제는 운영 전환 범위에서 처리합니다. 학생 답안과 보상 기록은 삭제하지 않습니다.
- 사이드 메뉴는 `f873978`의 공통 레이아웃 교체 때 AppShell 연결 및 관련 CSS가 제거되어 사라졌습니다. interface 설정 문제가 아닙니다.
- 교사 화면에서 768–1279px은 72px 축약 메뉴, 1280px 이상은 248px 메뉴를 표시합니다. 축약 메뉴를 펼치면 본문 여백도 248px로 바뀌어 가려지지 않습니다. 모바일·학생 메뉴는 기존 경로를 유지합니다.
- 현재 Header의 메뉴 설정, 권한 필터, 공휴일 관리자 예외 및 URL 정규화를 재사용합니다. 사이드 메뉴와 상단/하위 메뉴를 중복 마운트하지 않으며 알림·세션·로그아웃 컨트롤은 유지합니다.

검증은 `verify-teacher-sidebar-browser.mjs`의 격리된 실제 Header/MainLayout 브라우저 검사(390/768/1024/1280/1440px, 펼침·이동·본문 겹침·가로 넘침·학생 분기), 기존 shell/표시 계약, query purity, TypeScript 기준선, format, build로 구성합니다. 격리된 브라우저 검사는 Firebase에 연결하지 않습니다. 최종 테스트 사이트 반영 및 원격 파일 대조 결과는 배포 기록에서 별도로 확인합니다.

이 요청은 기존 표시 동결 계약 중 교사 사이드 메뉴 및 교사용 수업 화면에 대한 명시적 변경입니다. 삭제된 화면의 클래스만 예외에 추가하며 다른 화면의 기준은 낮추지 않습니다. 이전 소스의 전체 화면 증거를 새 소스 검증으로 재사용하지 않습니다.
