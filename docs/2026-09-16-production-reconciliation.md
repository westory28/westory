# 2026-09-16 운영 배포 복구 범위

## 확인한 문제

전날 점검 패치의 운영 반영은 완료됐으나, 이후 main `9c68015`의 Git 자동 배포가 운영 도메인을 교체했다. 해당 배포는 기존 Firestore 직접 쓰기 UI를 포함하며, 현재 운영 서버의 명령·세션·학기 규칙과 맞지 않는다. 실제 공개 파일에서 전날 점검 패치가 빠진 것도 확인했다.

## 복구 기준과 보존 항목

- 기준 소스는 검증된 저장 구조와 점검 패치, 학교 배너 후속 수정까지 포함한 `d360227`이다.
- 원본 `C:\westory`와 공유 `C:\westory-w10p`의 사용자 작업은 수정하지 않고 별도 checkout에서 통합한다.
- 최근 main의 달력 사용자 기능을 보존한다: 추석 본날/연휴 명칭, 일정별 색상, 연속 라벨·말줄임·제목 툴팁, 날짜 칸 경계, PC/태블릿 편집창, 토요일 공휴일의 빨간 날짜.
- 달력 저장은 기존 W8 명령·세션·멱등 처리 경로를 유지한다. 구형 직접 쓰기 또는 별도 호환 adapter로 교체하지 않는다.
- 일정 색상은 선택적 계약으로 확장한다. 이전 클라이언트가 생략하면 저장된 값을 유지한다.
- 운영 현재 학기의 구형 calendar 22건은 모두 공휴일이며 일반 일정은 0건이었다. 데이터 이전을 수행하지 않는다.
- 운영 root/current-semester 수업 자료는 모두 없음을 별도 저장 점검 작업에서 확인했다. 복구 과정에서 임의 데이터 삭제·이전은 수행하지 않는다.
- 별도 저장 점검 작업이 확인한 개발자 일지의 재인증 누락도 기존 권한을 유지하며 보완한다.

## 재발 방지와 반영

Vercel 운영 도메인의 자동 연결을 끄고, 정확한 커밋의 CI·소스 archive·배포 파일·Firebase 연결을 확인한 후보만 명시적으로 승격한다. 달력의 새 색상 계약을 받는 `executeCommand` 함수를 같은 커밋에서 먼저 배포하고 ACTIVE 상태를 확인한다. 이후 프런트 후보를 승격하고 운영 파일 대조와 실제 저장 확인을 마친 뒤 배포 결과를 기록한다. 학생 접근 잠금은 이번 복구에서 변경하지 않는다.
