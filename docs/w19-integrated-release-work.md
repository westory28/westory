# 출시 실행 묶음 — 진행 기록

기준 출발점은 `49812ae7d8960010bc0f12ae95e697e9fc418528`입니다. 작업 위치는 `C:/westory-w10p`, 대상은 `westory-staging-177587430482`입니다. 이 문서는 진행 기록이며 배포 완료나 출시 승인 기록이 아닙니다.

## 구현과 확인

| 영역 | 이번 묶음에서 확인한 실제 문제와 처리 | 현재 검증 |
| --- | --- | --- |
| 수업 파일·재인증 | 업로드 전부터 최종 문서 저장까지 하나의 제출 작업으로 보관하고, 실패하면 선택한 File과 초안을 복구합니다. PDF만 저장할 때 별도로 편집한 제목도 보존합니다. 응답이 유실되면 원래 payload와 receipt로 재확인하고, 복구 후 교체·삭제한 첨부의 불필요한 ticket은 제외합니다. | 실제 편집 컴포넌트의 격리 브라우저 7개 흐름 통과. 문서 반영 1회·추가 업로드 0회·초안 보존 및 390/768/1440px 검사 통과 |
| 학생 풀이 | 새로고침 뒤 첫 빈칸 입력이 native 이벤트와 React 이벤트 충돌로 소실되던 문제를 수정했습니다. | 실제 Note 라우트·풀이·각주·보상·계정/학기 격리·390/768px 검사 54개 통과. Firebase 연결 검증과 구분 |
| 성적 파일 저장 | 공식 XLSX 저장은 이미 구현돼 있었습니다. CREATE 재시도 후 새 로컬 ID 대신 서버가 반환한 원래 rosterId로 선택·캐시·학생 기록 연결을 갱신합니다. | 수행/지필 최초·재시도·실패 보존·101명 차단 및 기존 intent 검사 통과 |
| 지도 관리 | 기존 분류와 공유 fallback 모델을 유지하며 서버 저장·정렬·태그·탭·업로드·삭제에 연결했습니다. | metadata 11개, transport/만료 청소 8개, 첨부 교체 복구 9개 통과. 통합 브라우저·Staging 검증 남음 |
| 사료 관리 | 공유 원본의 업로드·메타데이터·참조 확인 후 삭제를 연결했습니다. 이전 revision 보존, 지연 processor의 재생성 방지, 삭제 실패 후 정리를 보완했습니다. | 서버 50개·클라이언트 복구 7개 검사 통과. 실제 Staging 업로드·처리·삭제 검증 남음 |
| 사료 각주 | 공유 원본을 교체해도 기존 수업의 같은 출처/path 조합은 유지합니다. 새 연결은 최신 검증 파일만 허용하고 삭제된 자료는 거절합니다. | 수업 서버 검사 28개 통과(기존 경로 보존·위조 경로·삭제 상태 포함) |
| 역사사전 원본·요청 | 공유 뜻풀이 저장과 학생 요청 처리를 같은 transaction으로 묶었습니다. 대상 오류나 101건 이상 요청이면 원본도 변경하지 않습니다. | 실제 함수 본문을 실행하는 72개 검사 통과 |
| 역사사전 승인 알림 | 요청 해결과 같은 transaction에 outbox를 저장합니다. 별도 생성 이벤트가 전달하고 실패·중단은 주기적으로 재시도합니다. 알림 지연이 저장 응답을 막지 않습니다. | 전달 실패·대기·응답 유실·중복·동시 lease·중단 복구 단위검사 통과. 실제 사용자 발송 검증은 하지 않음 |
| 역사사전 보상 출처 | 업무 학기와 분리한 여러 지급 출처의 조회·검증·추가·회수 계획 helper를 준비했습니다. legacy-point와 canonical Wis를 혼동하지 않습니다. | 순수 helper 94개 통과. 실제 writer/이전/공식 Wis 연결은 아직 미완료 |

## 남은 필수 조건

- 수업 저장·결과 재확인·첨부 교체는 격리 브라우저에서 검증했습니다. 새 배포에서는 실제 Firebase 연결 경로를 추가로 확인합니다.
- 지도·사료의 실제 작은 PDF/이미지 업로드, 교사 편집·재인증·학생 조회를 Staging에서 확인합니다. source processor의 처리 실패·만료·삭제 경합도 포함합니다.
- 역사사전 단건 CAS·Gateway 결과 복구, 원래 보상 학기/원장 출처 이전, legacy 보상과 현재 Wis 계좌 연결을 완료해야 합니다. 요청·삭제 알림은 아직 기존 직접 전달 경로입니다.
- 학생·학기·평가·Wis·일정·출석·공지·교사 메모의 기존 완료 근거와 실제 경로를 대조한 뒤 출시 후보 검증을 수행합니다. 현재 Functions 기본 검사 전체는 통과했으나 이것만으로 실제 업무 전체 통과를 뜻하지 않습니다.
- 타입 기준선의 기존 44개 오류를 완전한 타입 통과로 보고하지 않습니다. 신규 진단은 수정하고 결합 검증에서 재확인합니다.
- 검증된 Staging 후보의 백업/복원과 전환 리허설, 구체적인 운영 배포/복구안을 준비합니다. Production 접근 금지와 main 미병합 원칙은 유지합니다.

작은 수정·단위검사·커밋·중간 배포는 작업 종료 기준이 아닙니다. 배포 후보 및 운영 전환안을 준비할 때까지 같은 실행 묶음으로 이어갑니다.

## 첫 통합 Staging 배포와 실제 연결 확인

- 커밋 `57cb272805ce67684c06b7d77f70d020c88262bd`, tree `f1287a17c3edf90577363117052b8c9bea42a7dd`를 기능 브랜치에 푸시했습니다. 원본/main/Production은 변경하지 않았습니다.
- 중간 Staging 빌드 1회와 Functions 14개 배포, Hosting 배포를 완료했습니다. Hosting version `654134fafce6000e`, 원격 파일 133개 및 루트 문서 총 134개 비교에서 불일치 0입니다. 최종 후보의 독립 빌드 검증과 구분합니다.
- 실제 SDK/Auth/App Check를 사용한 지도 이미지·PDF 준비/업로드/연결/학생 조회/교사 수정/CAS/중복 요청/삭제가 통과했습니다. `batch2-maparchive-mtwv8nhi-6440bb8f0d-result.json`의 지도 항목이 근거입니다. 같은 실행의 사료 항목은 실패했으므로 파일 전체를 PASS로 해석하지 않습니다.
- 실제 사료 transport가 검증된 `_session` 필드를 일반 업무 필드로 잘못 거절하는 결합 오류를 발견했습니다. 인증 이후 두 transport의 허용 목록에 `_session`만 추가하고 58개 검사를 통과했습니다. 위 커밋의 고정 Functions snapshot에 수정한 `sourceArchiveManagement.js`만 반영해 업로드·정리 함수 2개를 별도 배포했습니다. 해당 파일 SHA-256은 `34f4faa7bbd704e1918675e9dbf627168c689d5c310dee8d93057f8985d17d2f`입니다.
- `batch2-maparchive-mtwvouak-35ae3ac074-result.json`: 사료 이미지·PDF 실제 처리/원본 hash/교사 조회/학생 직접 조회·쓰기 거부/수정/CAS/원래 receipt 재시도/수업 참조 삭제 차단/삭제 및 파일 정리 PASS입니다. 첫 실패 실행이 남긴 정확한 합성 tombstone도 정리했습니다.
- 합성 계정은 비활성화·토큰 폐기했고 임시 App Check 등록은 삭제했습니다. 실제 사용자 알림 호출 0, Production 접근 0입니다. 실행 기록은 `C:/westory-w10p-provenance-runtime`에 있습니다.

사전 8개 쓰기의 Gateway·CAS·결과 복구, legacy/canonical 보상 출처와 원장 연결, 구매 메모 저장은 다음 소스 묶음에서 통합 검증 중입니다. 아직 배포 후보 완료로 판정하지 않았습니다.

## 두 번째 통합 소스 — 사전·위스·전환 보호

- 사전 8개 쓰기를 서버 Gateway·동시 수정 버전 검사·원래 요청 결과 재확인으로 연결했습니다. 학생 입력과 교사 수정은 계정별 메모리 초안으로 보존합니다. 기존 callable은 새 화면 사용을 안내하며 쓰기를 거절합니다.
- 공식 뜻풀이와 학생 요청·알림 outbox를 같은 transaction에 저장합니다. 거절 후 재요청도 알림을 다시 만들고, 같은 commandId 재전송은 추가 전달을 만들지 않습니다. 최초 저장일은 재저장 시 보존합니다. 실제 사용자에게 알림을 보내는 검사는 수행하지 않았습니다.
- 과거 사전 보상은 실제 지급 학기·원장을 확인합니다. 현재 보상은 canonical Wis에 지급하며, 과거 잔액이 이전된 경우에는 원본을 보존하고 원래 canonical 계좌에 결정적 LEGACY_RECLAIM을 기록합니다. 일반 회수와 같은 계좌의 누계에 함께 반영합니다.
- 같은 학기의 legacy 잔액 이전은 원본 hash, UID, 거래별 잔액 흐름, 잔액·획득·순위·소비·조정 누계와 미처리 주문을 검사합니다. 빈 canonical 계좌에만 opening을 기록하고 marker 재실행은 입금하지 않습니다. 초기 지급을 추가로 요구하거나 두 번 지급하지 않도록 초기화 집계를 함께 갱신합니다. 현재 이전 adapter의 프로젝트 허용 범위는 Staging/demo뿐입니다.
- 이전 control은 일반 Wis, 수업 보상, 사전 보상과 legacy 학생 자료 정리의 같은 transaction에서 확인합니다. 이전 완료된 legacy 금융 원본은 학생 프로필 갱신·정리에서도 감사 근거로 보존하고, 나머지 학생 자료 처리는 계속합니다. 관리용 Admin SDK 정비 스크립트를 임의로 실행하는 것까지 차단한 근거는 아닙니다.
- 구매 메모의 서버 저장·학생/교사 조회·원래 payload 재시도를 연결했습니다. 기존 메모 없는 receipt의 hash 계약은 유지합니다.
- Staging 지도 설정을 별도로 준비했습니다. Maps Embed API만 허용하며 Staging의 firebaseapp.com/web.app 두 도메인으로 제한합니다. 기존 Firebase 공개 설정은 바꾸지 않았습니다. Maps 설정 SHA-256은 `c8a37bc3416e3bf4230cf4f291a43e47a7cca1d32ef75595c243af98af0c2061`입니다. 결제 설정 변경과 Production 접근은 0입니다.
- 색인 CLI 배포가 Rules 검사 서비스의 503으로 두 번 실패하여, Firestore 관리 API로 검토한 색인 3개만 생성 요청했습니다. 기존 색인 삭제와 Rules 변경은 하지 않았습니다. 완료 상태와 실제 조회는 배포 후 확인합니다.

검증 근거: Functions 기본 검사 통과, 사전 Gateway 통합 186개, legacy 보상 172개, canonical 보상 296개, 잔액 이전 59개, 이전/rebuild 통합 170개, legacy 정리 fence 7개 통과입니다. 교사 실제 React 편집 화면의 격리 브라우저 1,204개 검사와 가져오기 복구 150개 검사도 통과했습니다. 격리 브라우저는 실제 Firebase 연결 근거와 구분합니다. 클라이언트 쓰기 경계는 143개 승인·UNKNOWN 0·기존 fixture 17개 통과이며, 상세 검토는 `w10p-batch3-client-write-boundary-review.md`에 있습니다.

아직 필요한 작업: 새 배포의 실제 사전 지급·회수·합성 알림 및 지도 표시 확인, 전체 업무 흐름 중 실제 사용 미확인 항목 검증, Staging 백업·복원·잔액 이전 리허설, 출시 후보 독립 빌드와 전환·복구안입니다. 운영 데이터 조회·이전·배포는 실행하지 않았습니다.

## 두 번째 실제 배포와 통합 업무 확인

아래 기록이 위 중간 단계의 미확인 항목보다 최신입니다. 전체 출시 후보 완료 선언은 아닙니다.

- 소스 `96072114c16c102f372126e073addc7913c1f334`, tree `850ac40575864d4e422fb89eae9452f5a096cbe6`에서 Functions 17개와 Staging Hosting을 배포했습니다. Hosting version `93a6ecde2968077d`, release `sites/westory-staging-177587430482/releases/1789128621501000`입니다. 실제 파일 133개·루트 문서 포함 134개 비교에서 불일치 0입니다. 독립 빌드 2회에 의한 최종 후보 판정과 구분합니다.
- `batch3-dictionary-mtwx289t-588caa707e4d8bda2747-result.json`: 실제 Firebase SDK/Auth/App Check로 사전 7개 안전한 명령 경로, 지급 50·회수 50·원장 2건, 같은 요청 재전송·새 요청 중복 지급 방지, 버전 충돌 거부, 승인 요청 처리와 합성 수신자 3건 전달/조회가 통과했습니다. 실제 교사 전체에 알림을 보내는 학생 요청 생성 명령은 실계정 발송 없이 별도 검사 근거로 구분합니다. 정리 오류 0입니다.
- Maps Embed는 westoria28의 Chrome 프로필에서 새 배포를 열어 실제 Google 지도 표시를 확인했습니다. 기존 탭의 이전 번들 캐시와 새 배포를 구분했습니다.
- `b3ops-mtwxfhbb-9bf8c2777ecd5136a61a-result.json`: 실제 SDK/Auth/App Check로 18개 업무 흐름이 통과했습니다. 구매 메모 500자 저장/501자 거부, 주문 승인·완료·거절·환급·재전송, 잔액/원장/순위 누계 대조, 일정 생성·수정 충돌·학생 조회·보관, 출결 일괄 저장·재전송·정정 이력·다른 학생 조회 거부·마감, 교사 개인 초안 저장·충돌·폐기를 확인했습니다. 합성 계정 3개 비활성화·토큰 폐기, 임시 App Check 제거, 정리 오류 0입니다. 교사 개인 패치 메모 및 공지 발행 검증과는 다릅니다.
- 교사 개인 패치 메모는 기존 `patch01-live-result.json` 실제 SDK 증거를 재사용합니다. 현재 `teacherPatchNotes.js`와 `firestore.rules`가 당시 소스와 Git blob까지 같음을 확인했습니다. CRUD·동시 수정·타인 접근 거부·삭제 후 이전 요청 재확인 근거이며, 최근 구현을 다시 만들지 않았습니다.
- 첫 이전 리허설은 합성 원본의 typed 백업·삭제 후 복원·원본 hash 일치까지 확인했으나 검증 도구의 거부 처리 중 종료됐습니다. 해당 실행은 실패로 기록합니다. 정확한 소유자와 원래 설정을 대조해 임시 공유 설정 2개를 복원하고 합성 계좌를 닫았습니다. `migration-rehearsal-mtwx64c0-19c2205bbc56b89f-recovery.json`이 복구 근거입니다. 운영 접근은 없었습니다. 이후 VM 거부 검사, 안전한 예외 수렴, Timestamp 정밀도와 maintenance 스키마를 보완해 재실행할 예정입니다.

운영 전환 준비안은 `runbooks/2026-09-release-transition.md`에 정리합니다. 기존 학기 전환 문서의 0원 계좌·새 학기 활성화 절차를 이번 동일 학기 업데이트에 그대로 적용하지 않습니다. 현재 운영 상태는 승인 전 미조회입니다.

## 명부 승인 정책과 추가 실증

사용자는 신규 학생 등록을 **교사가 명부를 확인한 뒤 승인**하는 방식으로 확정했습니다. 새 프로필은 PENDING으로 생성하며, 교사가 학급·번호·이름을 확정한 후 현재 학적과 빈 Wis 계좌를 준비하고 APPROVED로 전환합니다. 중간에 실패하면 APPROVED_PENDING_ACCOUNT에서 이어서 처리합니다. 초기 지급은 하지 않으며, 승인 전 수업·성적·파일·업무 명령 접근과 학생의 자기 승인·승인 표시 제거를 차단합니다. 승인 정보가 없는 기존 학생은 보존합니다.

기존 학생의 프로필 수정·반 이동·진급은 명단 조회 당시 버전으로 검증하고 users/identity/현재 학적/현재 Wis 표시를 같은 transaction에 갱신합니다. 성적·거래·원장·금액을 과거까지 다시 쓰지 않습니다. canonical 학적이 있는 학생은 기존 updateStudentData 경로로 이 검증을 우회할 수 없습니다. 전체 후보의 운영 반영 완료를 뜻하지 않습니다.

- `migration-rehearsal-mtwxlzj3-b418e9563353763d-result.json`: 재실행 13단계 통과, typed 백업·삭제 후 복원 hash 일치, 이전 중 쓰기 차단, commit 후 중단·재개와 중복 입금 방지, 공유 설정 원상 복원·정리 오류 0입니다. 실제 Staging DB와 Gateway core를 사용했으며, 권한은 합성 주입했으므로 실제 Auth/App Check/HTTPS 증거와 구분합니다.
- `b3notice-mtwxprjo-f1639143cc6f488ab7b7-result.json`: 실제 SDK/Auth/App Check로 공지 생성·수정 충돌·발행·같은 요청 재전송·지정 학생 1명 전달·다른 학생 접근 거절·읽음 중복 방지·보관을 확인했습니다. 실제 사용자 수신 0, 합성 계정 정리 오류 0입니다.
- `grade-xlsx-mtwylptk-3ced8856110246b9-result.json`: 수행평가와 서술형 성적의 실제 XLSX 4개를 기존 파서·저장 직렬화 경로와 SDK로 검증했습니다. 신규 저장·수정·버전 충돌·다른 학적 거부·학생별 조회·같은 명령 재전송 시 중복 성적/버전 생성 방지가 통과했습니다. 응답 유실은 성공 결과를 클라이언트에서 버리는 방식으로 재현했으며, HTTP 자체를 끊거나 React 파일 선택 화면을 검사한 것은 아닙니다. 기능·정리 모두 PASS입니다.
- 신규 승인 서버 57개·접근 경계 19개, 기존 maintenance 서버 44개 검사를 통과했습니다. Rules 변경 뒤 교사 패치 메모의 이전 서버 로직 증거는 재사용하되, Rules가 당시와 같다는 위 기록은 변경 전 시점에만 해당합니다. 변경된 Rules는 별도 회귀검사와 Staging 배포 검증 대상으로 관리합니다.

실제 신규 승인·학적 수정의 Staging 연결, 새 Rules/전체 callable guard 배포, 독립 빌드와 최종 화면 검증은 이어서 수행합니다. Production 조회·배포는 수행하지 않았습니다.

배포 전 통합 검사에서 Functions 기본 검사, 학적 서버 44개·클라이언트 10개·transpile 6개, 승인/maintenance/교사 패치 메모 Rules, 성적 확인 bootstrap 및 직접 쓰기 차단을 통과했습니다. 타입은 기존 기준선 44개/12파일·신규 0입니다. 클라이언트 경계는 기존 142개 항목을 보존하고 공통 Gateway 호출 지문 1개와 사용자 클릭 기반 명령 2개만 반영해 145개 승인·UNKNOWN 0입니다. 새 조회 2개는 서버 읽기 전용과 대조했습니다. Rules 에뮬레이터의 일부 거부 응답에는 1,000-expression 진단이 함께 나타났으며, 정상 생성·수정·조회 허용 경로와 의도한 거부를 별도로 확인했습니다. 에뮬레이터 종료 시 Storage Java EOF 경고는 애플리케이션 실행 오류와 구분합니다.

## 승인 정책 실제 연결과 후보 14053dd

- 사용자가 신규 학생 정책을 **교사의 명부 확인 후 등록 승인**으로 확정했습니다. 실제 SDK의 가입·세션·PENDING 프로필 생성, 학생 업무 접근 거절, 교사 승인 → 0원 계좌 준비 → 최종 승인, 단계별 같은 명령 재전송, 승인 후 접근, 이름·번호 수정과 반 이동을 확인했습니다. 기존 승인 기록·금액·원장은 보존됐습니다. `b3reg-mtwzh2nc-096f1e305bce346c-result.json`은 기능과 합성 계정 정리 모두 PASS입니다.
- `14053ddd2330f3e64f5b545c0ba834b810998ec4`에서 두 별도 git archive와 잠금 파일 기반 설치·빌드를 수행했습니다. 정적 파일 133개가 byte 단위로 일치했습니다. manifest SHA-256은 `14ca6b87ce51184df4c161a3d261229797c72e0563adf9cd85c48ba434fbab55`입니다. Hosting version `15b6df65939ce103`, release 시각 `2026-09-11T13:20:28.565Z`; 파일과 루트 문서 총 134건의 원격 비교에서 불일치 0입니다. 이 후보는 화면 검증 중 발견된 수정의 **출발 후보**이며 최종 출시 승인본은 아닙니다.
- 공통 가입 승인 guard를 사용하는 callable 63개를 Staging에 배포했습니다. 새 trigger 배포는 없었습니다. `batch4-callable-deploy-receipt.json`과 `batch4-remote-backend-audit.json`에 대상·소스·원격 상태를 기록했습니다. 후속 공식 GET 6회에서 63개 모두 ACTIVE와 entryPoint/source 참조 일치, 추적 소스 88개의 로컬 SHA 일치를 확인했습니다. 원격 Functions 압축 소스 자체의 byte 비교는 하지 않았습니다.
- Firestore의 실제 release는 ruleset `8d5d4492-8bc3-4ef2-81b0-dd61299d645a`, 원문 SHA-256 `145a2f1376a5e85688df398ebedbfe40092393f7480a81306403ea668e95f30d`입니다. Storage 원문 SHA-256은 `16707f08092bcdc1f6eb7dd96dcec7a6ccf79177b65bd90b63c969957b0000bf`입니다. 두 원격 원문 모두 검증 파일과 일치합니다. Firestore 관리 서비스의 일시적 503은 규칙을 완화하지 않고 동일 후보 재검증·공식 API release로 해결했습니다.

## 실제 화면 대조 중 확인한 사항

- 실제 Chrome 방재석/westoria28 프로필에서 교사 화면을 확인했습니다. 앱 이메일 입력값은 실제 이미지에서 정상 표시됐습니다. CUA의 DOM/value 관찰에서 이메일이 빈 값으로 반환된 현상은 앱 데이터 유실로 판정하지 않았습니다.
- 학적 상태가 BLOCKED인데 수정 버튼이 활성화됐던 안내 결함을 발견했습니다. 서버 및 클라이언트 저장 경계는 이미 차단하고 있었으며, 화면에도 사유와 비활성 상태를 연결합니다.
- 역사 사전 1280px, 역사교실 필터 1024px, 학습·공지의 좁은 날짜 입력, 긴 위스 이름·거래 사유의 모바일 가로 넘침을 실제 측정했습니다. 기존 화면 구조를 유지하는 반응형 수정 대상으로 좁혔습니다.
- 오래된 관리자 인증으로 학기 준비도를 조회하면 최근 인증 필요 오류가 일반 네트워크 오류로만 보였습니다. 기존 본인 확인을 거쳐 준비도만 재조회하는 복구 버튼을 연결하고 설정 초안은 보존합니다.
- 일정의 불투명한 학급 ID가 학년·반처럼 분해되어 표시되는 문제와 기존 복수 대상의 편집 보존을 확인 대상으로 추가했습니다. 운영 학급·학생·일정은 읽거나 수정하지 않았습니다.
- 첫 5크기 자동 캡처에는 화면 크기 변경 직후 이전 배치를 찍은 이미지가 섞였습니다. 초기 결과는 진단용이며 최종 시각 PASS로 계산하지 않습니다. 실제 CSS 크기·스크린샷 비율·로딩 종료·가로 넘침을 확인한 증거로 교체합니다.

성적 화면의 `(이름 없음)`은 테스트 작성 시 합성 학적의 `snapshot.displayName`을 누락한 것이 원인이었습니다. 원래 XLSX 이름은 정확했지만 서버가 공식 학적 스냅샷으로 정규화하면서 빈 값이 저장됐습니다. 계정 폐쇄로 성적이 지워진 것은 아닙니다. 테스트 학적 계약과 이름 검사를 보완한 `grade-xlsx-mtx0elj8-8665d9fdef567b57-result.json`은 실제 XLSX 저장·수정·조회·충돌·중복 방지와 계정 폐쇄 전후 성적 불변성 모두 PASS입니다. 앞선 실행의 이름 검증 누락을 이 결과로 보충합니다.

새 학생 화면용 합성 계정은 승인까지 완료했습니다. 학습 카드의 즉시 조회는 로컬 공개 시작 시각이 서버 저장 시각보다 약 2.3초 미래여서 실패한 것으로 진단했습니다. 원래 실패 기록은 유지하고 실제 학생 화면에서 공개 기간 내 조회를 추가 확인합니다. 자동 브라우저 첫 실행은 잘못된 로그인 라우트와 App Check의 headless 거절로 본문 검사 전에 중단됐으므로 110개 학생 화면을 통과했다고 계산하지 않습니다. 기본 봇 검사와 공식 임시 테스트 토큰을 사용하는 격리 검증의 근거를 구분합니다.
### 최종 화면 보완 묶음 — 2026-09-11 검증 진행

- 새 학생 등록 정책은 교사의 명부 확인 후 승인이다. `14053dd`의 실제 SDK 검증에서 승인 전 업무 접근 차단, 승인·0 Wis 계좌 준비·확정의 재시도 및 기존 학생 호환을 확인했다.
- `student-ui-1789135049501-b3ui-mtx02ete-99704cb61a761291/result.json`은 실제 앱 로그인으로 22경로×5크기=110건을 검사했다. 콘텐츠 60, 정상 빈 상태 35, 실행 문맥 없는 경로 10, 지난 학기 안내 권한 오류 5건이다. 가로 넘침은 없었다. 110건 전체를 기능 성공으로 집계하지 않는다. 공식 임시 App Check debug 등록은 삭제 후 404를 확인했다. 실제 Google/reCAPTCHA 검증은 관리자 브라우저 증거와 구분한다.
- 지난 학기 안내는 허용된 종료 학기 메타데이터만 조회하도록 수정했다. 학생 개인의 과거 성적·자료 열람 권한을 새로 부여하지 않는다. 8개 동작 및 2개 구문 검증을 통과했다.
- 설정 화면은 정확한 `RECENT_AUTH_REQUIRED` 응답에 재인증 후 다시 조회하는 경로를 제공한다. 입력 중인 설정은 보존하고, 사용자·학기 변경 및 중복 클릭은 차단한다. 10개 시나리오 및 구문 검증을 통과했다.
- 일정 화면은 현재 명부의 실제 학급을 표시하고 기존 복수 학급·개별 대상 제한을 보존한다. 제목만 수정할 때 원시 시작/종료 시각·종일 여부·교시·일정 종류·설명도 그대로 유지한다. 실제 조회→편집→저장 함수 회귀검증을 통과했다. 실제 관리자 화면 저장 후 원격 문서 대조는 새 후보 배포 후 수행한다.
- 학생 정보 수정은 준비되지 않았거나 BLOCKED인 명부 상태에서 입력·저장을 막고 이유를 표시한다. 14개 동작 및 6개 구문 검증을 통과했다. 메일 표시가 도구의 DOM 결과에서 비어 보였지만 실제 화면에는 정확히 표시됨을 확인했다.
- 사전·역사교실·학습/소통 폼·위스 내역의 작은 화면 넘침을 최소 CSS 변경으로 보완했다. 최종 후보에서 영향을 받은 화면을 다시 확인한다. 백엔드·Rules·의존성 변경은 없다.

### 9월 12일 실제 조작에서 확인한 저장 결함과 후속 후보

- `1852f5d3bcef6bb87a0939f825d8e4d32147c84a`의 독립 빌드 2회는 정적 파일 133개가 일치했다. Hosting version `0eb88145c4a665f2`, release `1789137147224000`, 원격 파일/루트 비교 134건 불일치 0이다. 이 후보는 아래 답안 자동저장 결함을 발견한 검증 후보이며 최종 출시본으로 사용하지 않는다.
- 실제 westoria28 Google 재인증에서 ProtectedBoundary가 설정 화면을 다시 마운트해 초안을 잃는 문제를 수정했다. UID·학기·10분 TTL에 묶인 메모리 초안을 복원하고 준비도만 다시 조회한다. 실제 계정에서 퀴즈 설정과 연도 입력이 유지됐으며 설정 저장은 하지 않았다. 정상 준비도 보고서가 아직 없다는 상태와 재인증 실패를 구분했다.
- 실제 관리자 일정 제목 저장 후 학급·개별 대상과 원시 시각·종일·교시·종류·설명 보존을 원격 대조했다. BLOCKED 학생 입력 차단, 승인 학생 수정 가능, 수행평가 두 학급의 공식 이름 연결도 실제 화면에서 확인했다.
- `b5runner-mtx27nvh-9cf2967b413ec9fe` 실제 SDK 평가/역사교실 검증은 시작·답안 저장·재로그인·제출·같은 요청 재시도와 결과 보존을 통과했다. 그러나 `b5runnerui-mtx2vsrq-91593d5a` 실제 UI 검사에서 퀴즈 단일 선택 후 자동저장이 이전 렌더의 빈 답안 `{}`를 보내는 결함을 발견했다. SDK 성공을 UI 성공으로 확대하지 않는다. 실패한 첫 UI 실행과 기존 응시는 보존했다.
- QuizRunner는 지연 저장과 화면 종료 저장에서 최신 렌더의 저장 함수로 위임한다. 저장 중 추가 입력은 후속 저장하고, 늦은 응답이 다른 응시·제출 완료·더 높은 revision을 덮지 않도록 제한했다. 실제 소스의 입력/저장/복구/cleanup 함수를 실행한 회귀 10개와 TSX 구문 2개를 통과했다. 기존 직접 호출 추적 경계를 유지해 허용 목록과 trigger 분류는 변경하지 않았다. HistoryClassroom은 답안 의존 effect가 최신 저장 타이머를 만들므로 같은 결함이 없었고 코드를 바꾸지 않았다. 새 후보의 실제 UI 재검증 결과는 후속 최종 판정에 연결한다.
- 교사 최소 권한 캡처의 접근 거부는 W9 읽기 권한과 담당 학급 미배정 상태를 구분한다. 정상 담당 교사 검증을 위해 합성 계정의 최소 읽기 권한/담당 합성 학급만 임시로 연결하고 원상 복구한다. 서버의 담당 학급 경계를 완화하지 않는다.

### 평가 보상 연결 — 실제 제출에서 발견한 공백 보완

- `e92f434`는 독립 빌드 2회와 원격 정적 파일/루트 134건 비교를 통과했다. 실제 UI의 만료 응시는 TIMEOUT 제출과 0점 결과 저장까지 성공했지만, 퀴즈·역사교실이 폐기된 보상 함수를 호출하는 공백을 발견했다. 이 후보를 최종 출시 완료로 판정하지 않는다.
- 제출 결과, 기존 정책의 기본/성과 보상, 위스 원장·잔액·순위 투영 및 명령 영수증을 같은 서버 트랜잭션에 연결했다. 보상 검증 실패는 전체 제출을 취소하며, 정책 비활성·지급 간격 제한은 정상 제출로 처리한다. 기존 제출에 소급 지급하지 않고 같은 요청 재전송은 추가 지급하지 않는다. 자동 보상은 기존 정책대로 earned/rank 누계에만 반영하며 adjusted 누계는 유지한다.
- 학생 화면은 서버 확정 보상만 표시한다. 재전송으로 복구한 결과에는 새 지급 효과를 반복하지 않는다. 위스 내역에서 퀴즈·역사교실 기본/성과 보상 종류를 보존한다. 지도 태그의 별도 보상 연결은 이번 평가 수정에 포함하지 않았으며 남은 기능으로 명시한다.
- 실제 Gateway·평가·위스·정책 함수 통합 37개, 클라이언트 확정 보상 22개, 답안 최신 렌더 회귀 10개 및 기존 Functions 검사 통과. TypeScript baseline은 기존 44건/12파일, 신규 오류 0이며 전체 타입 오류 0을 뜻하지 않는다. 직접 쓰기 경계 unknown 0과 앱 포맷 검사를 통과했다.
- 다음 검증은 Staging의 executeCommand/getWisEconomyState 및 추가 색인 2개 반영, 새 고정 소스의 독립 빌드/원격 대조, 실제 UI 정답 제출 2건과 원장·잔액·재전송 대조다. 이 문단 작성 시 해당 원격 검증은 아직 완료되지 않았다. Production 접근·배포는 수행하지 않는다.

### 9월 12일 최종 Staging 후보와 실제 검증 결과

- 화면 소스는 `c11d1635df5d0f213d8b2e253906009595b902e8`이다. 두 독립 빌드의 정적 파일 133개가 같았으며 manifest SHA는 `b4cd6f10487eb741b694fe07954f30b227f34fb9a3d043e65f308f2ce20d33c3`이다. Hosting version `fa942652ca99ca3a`, release `1789144377688000`, 배포 시각 `2026-09-11T16:32:57.688Z`; 원격 파일/루트 134건 대조에서 불일치 0이다. 이후 문서 커밋은 이 앱 소스와 구분한다.
- `executeCommand`, `getWisEconomyState` 두 함수는 보상 구현 소스 `0f431023bef32c48b9e7f84a3a21185b0e6f1b98`로 배포하고 공식 GET으로 ACTIVE/소스 참조를 확인했다. 나머지 61 callable과 trigger는 앞선 배포 조합을 유지한다. 모든 함수를 마지막 화면 SHA로 배포했다고 주장하지 않는다. Firestore/Storage Rules·공개 설정은 앞서 검증한 고정본 그대로이며 색인 17개 모두 READY다.
- 실제 학생 UI `b5runnerui-mtx5a5p6-415646fd/result.json`에서 퀴즈·역사교실 답안 저장, 새 로그인 복구, 100점 제출, 제출 후 재로그인, 기록 화면, 위스 내역을 확인했다. 37캡처, 기능 PASS, 미확정 0, 임시 App Check 삭제 후 404와 정리 오류 0이다. 앞선 자동검사의 버튼 식별 실패는 실제 접근성 이름 `제출하기 `를 정확 문자열과 비교한 도구 문제였으며, 원본 실패 기록과 응시를 보존하고 동일 응시를 이어 완료했다.
- 실제 정책의 퀴즈 10/역사교실 50, 보너스 비활성 상태를 그대로 유지했다. `batch8-reward-1789143267049-a9572b49-after.json`에서 canonical 원장 2개, 잔액·earned/rank +60, spent/adjusted 불변을 확인했다. UI의 원 commandId·payload 두 건을 공식 SDK로 재전송해 `replayed:true`와 추가 지급 0을 확인했다. 금융 snapshot hash는 전후 `d4ef8e265589f23b392dca1903242d06923028d351ecdcc74926efd768a3695e`로 같고, 학생 조회 결과와 immutable 응시/답안/성적/영수증도 같았다. 최종 증거 SHA는 `d13169f0d144cc51e829c0869ed8686de9e557477e49013c180461c0bd95e3bc`다.
- 모바일 실제 이미지에서 역사교실 결과창 z120 위로 풀이 도구 z140이 겹치는 문제를 발견했다. 기존 확인창과 같은 z150으로 맞추고 닫기 버튼을 한 줄로 유지했다. 후속 실제 재응시에서는 초기 빈 답안 저장과 정답 저장이 같은 revision으로 겹쳐 정답 요청이 409로 거절되는 별도 결함도 확인했다. 저장 요청을 순서대로 실행하고 최신 revision을 사용하며 제출은 대기 중 저장이 끝난 뒤 진행하도록 수정했다. 늦은 응답은 다른 응시·제출 완료·더 높은 revision을 덮지 않는다.
- 직렬 저장 실제 함수 회귀 10개, 기존 퀴즈 최신 렌더 10개, 서버 확정 보상 클라이언트 22개가 PASS다. DW-100은 동일 `saveAssessmentProgress` callable의 내부 호출 순서가 바뀐 것으로 검토했고 호출 그래프 hash만 갱신했다. 경계 종류·root count·trigger 분류·허용 기간·서버 권한은 확대하지 않았다. 최종 경계 145개/unknown 0/명령 28개, TypeScript 기존 44건/12파일·신규 0이다.
- 최종 후보의 `b5runnerui-mtx6efkr-bcc26e1d/result.json`은 바로 그 충돌 응시를 재개해 정답 저장 200/revision 2→3, 재로그인 복구, 100점 제출, 결과·기록·위스 조회를 PASS했다. 역사교실 재지급은 실제 24시간 제한에 따라 `NOT_ELIGIBLE/cooldown_active`, 새 원장 0, 잔액 60이었다. 결과창은 390/768/1024/1280/1600px 모두 도구보다 앞에 표시되고 닫기 버튼은 한 줄이었다. 9캡처, 미확정 0, App Check 정리 오류 0이며 Root가 최종 390px 이미지를 직접 검토했다. 증거 SHA는 `d7b0355edcfbb806e9dfae82346b0cbc212fe2e5ad3912e669399764d4a4d9bb`다.
- 평가 정의 2개를 공식 명령으로 CLOSED 처리하고 해당 합성 설정 키만 CAS로 제거했다. 원본 소스는 읽기 전용으로 닫고 결과·원장·감사 기록은 보존했다. 마지막 학생/교사 합성 계정 2개는 Auth 비활성화·refresh token 철회, 학적/학급/계좌 폐쇄 및 로컬 자격 증명 제거를 완료했다. 실제 관리자 계정은 변경하지 않았다.
- 정리 후 `batch8-fixture-closure-audit.json`의 읽기 전용 재조회에서 두 Auth 계정 비활성, 교사 임시 권한/담당 학급 원복, 학적 슬롯 제거, 계좌·투영 폐쇄와 금액/누계 불변, 원장·기존 결과/답안/영수증 보존, 마지막 0위스 재응시 결과 보존, 평가 정의/소스 폐쇄 및 로컬 자격 증명 파일 제거를 PASS했다. 확인 시각은 `2026-09-11T16:38:00.869Z`, 추가 원격 쓰기는 0이다.
- 실제 Chrome 방재석/westoria28 탭은 최종 `main-BG6m4AGV.js`를 확인했다. 장시간 유휴로 만료된 세션이 로그인 화면으로 복귀한 상태이며, 마지막 후보에서 새 Google 로그인을 대신 완료했다고 주장하지 않는다. 이전 실제 Google 재인증과 교사 화면 검증은 별도 근거다.

Staging 후보 확인과 Production 적용 완료는 구분한다. 이 시점의 잔여 항목은 아래 Batch11 결과로 갱신한다. 운영 현황·실제 자료 연결·백업/복원·배포는 실행하지 않았으며 [운영 전환 준비안](runbooks/2026-09-release-transition.md)의 읽기(A), 백업(B), 변경/중단 창(C) 승인 범위에 따라 진행한다. Production 접근·쓰기·배포는 0이다.

### 9월 12일 Batch11 — 지도 보상 연결 및 타입 오류 해소

- 최종 화면 소스는 `899f2bfe832e00bafe7c46835844e114c4b1c072`, tree `0fdace8a6e6627871f15ce1150c600d6de064b30`이다. 독립 빌드 2회의 정적 파일 133개가 일치했다. manifest SHA256은 `bb65da6bb28feb930a64406e6addeb73ecd6002e8cf79fa3d6150b7753983426`이다. Hosting version `90e2ed7143e2e40c`, release `1789183358499000`; 최종 원격 파일·루트 134건 대조에서 불일치 0이다. 배포 직후 Calendar 파일 404가 한 차례 있었으며, 실패 로그를 보존하고 재대조에서 전체 일치를 확인했다.
- `executeCommand`는 `d62b68c77c42337747a8e9d501a0215f477ec40f`, `getWisEconomyState`는 `41b9e4d664a80894047e5cf3e38c459cebddd4f9` 소스다. 두 함수는 각 배포 영수증·공식 원격 ACTIVE 메타데이터로 확인했다. 다른 함수와 Rules는 기존 배포를 유지했다. 모든 함수가 같은 소스라고 주장하지 않는다.
- 지도 태그는 폐기된 보상 호출 대신 `claimMapTagReward` Gateway를 사용한다. 학생·승인·현재 학기·학적·학급·계좌·지도 태그·이전 및 새 원장을 서버가 확인한다. 기본 10위스/24시간/최대 5회와 실제 정책을 유지하며 금액·학생 UID를 클라이언트에서 받지 않는다. 동일 요청은 영수증으로 복구하고 계좌·원장·영수증을 함께 확정한다.
- 실제 Chrome westoria28 프로필에서 새 합성 학생이 PDF 확대 화면의 태그를 클릭해 10위스를 받았다. 원 commandId/payload 재전송은 추가 0, 새 interaction의 cooldown 요청도 추가 0, 교사 요청은 거절됐다. 계좌·잔액·누적 획득·순위·원장·학생 조회가 일치하고 사용/조정 누계는 보존됐다. 최종 화면에서 `지도 태그 탐색`, 날짜, 10위스와 필터를 확인했다.
- 실제 배포 검증 중 영수증 `sourceHash` 누락으로 Firestore가 거래를 원자적으로 거절하는 결함을 발견했다. 원본 식별 hash를 반환하도록 수정하고 테스트 Store도 undefined 쓰기를 거절하도록 보강한 뒤 같은 UI 요청을 완료했다. 오류를 숨기는 설정은 사용하지 않았다. 합성 PDF의 전처리 이미지 누락은 테스트 자료만 보완했으며, 원자료·hash 보정 기록을 보존했다.
- 기존 TypeScript 12파일 44건을 해결했다. 최종 `tsc --noEmit` 오류 0/파일 0이며 과거 진단 허용 목록을 비웠다. 검사기 자체 실패도 통과로 처리하지 않는다. 기존 비활성 성적 화면은 유지하고 점수 계산을 바꾸지 않았다. 서버 Timestamp의 `_seconds/_nanoseconds`를 날짜 표시에 지원하고 지도 보상 분류 누락을 수정했다.
- 검증: 앱 빌드·변경 파일 Prettier·Functions 기본 검사·클라이언트 경계 146개/UNKNOWN 0 통과. 지도 서버 31개, 클라이언트 재시도, 평가 보상 37개, 일정 비교 841쌍, 성적 워크북 3종/합성 7명, 기존 거래 분류 141개, 날짜 80개를 확인했다. 실제 설치 Excel 모듈이 XLSX 파일을 생성하는 계약도 확인했다. 이 수치는 전체 사이트의 모든 경로 검증 수가 아니다.
- 새 합성 계정 2개를 종료하고 로컬 암호 파일·합성 지도·임시 App Check를 정리했다. 원장·영수증은 보존했으며 미확정 지도 요청 0이다. 관리자 Google 로그인도 확인했다. Production 접근·배포·실사용자 알림은 모두 0이다.
- 최종 판정: `C:/westory-w10p-provenance-runtime/batch11-final-release.json`. 지도 지급/재전송 근거는 `b3ui-mtxsp048-0a266945b6b3de96-batch11-map-qa.json`, 최종 화면은 `batch11-final-wallet.png`와 `batch11-final-wallet-dom.txt`다. 초기 실패·보정·재검증 기록도 같은 runtime 폴더에 남긴다.

고정 가중치 진행률은 **75 → 79 / 100 XP**다. 지도 보상 2 XP와 타입 오류 해소 2 XP를 반영했다. 공식 휴일 API 1 XP, 운영 읽기 점검 5 XP, 실자료·백업·복원 7 XP, 운영 적용·최종 확인 8 XP가 남는다. 공식 휴일 API는 Firebase Hosting 서버 연결이 없으며, 확인한 Staging 함수 설정에도 관련 키 참조가 없었다. 자동 생성 달력과 공식 최신 휴일 조회 성공은 구분한다. 다음 단계는 기존 A/B/C 승인 경계에 따른 **A 운영 읽기 전용 사전 확인**이다.
